[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --tb=short
    --strict-markers

markers =
    ui: UI tests
    api: API tests
    smoke: Smoke tests
    regression: Regression tests
